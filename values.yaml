images:
  airflow:
    repository: kimpass189/kim_airflow
    tag: 0.1.0

executor: LocalExecutor

extraEnvFrom: |
  - secretRef:
      name: airflow-api-keys

dags:
  persistence:
    enabled: false
  gitSync:
    enabled: true
    repo: https://github.com/Kimwonjoon/Twilight_dags.git
    branch: main
    subPath: ""

logs:
  persistence:
    enabled: false
